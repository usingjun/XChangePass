# 로컬 production 빌드(npm run build && npm run preview) 검증용 기본값.
# vite preview도 vite.config.ts의 server.proxy를 통해 /api -> localhost:8080으로 프록시하므로
# 이 값이면 로컬에서는 동작한다. 실제 원격 배포 환경에는 프록시가 없으므로, 그때는
# 배포 파이프라인에서 이 값을 백엔드 실제 오리진으로 덮어써야 한다(하드코딩 금지 원칙은 유지).
VITE_API_BASE_URL=/api
